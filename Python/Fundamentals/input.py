# input() is a built-in Python function used to receive data from the user through the keyboard.

# This will prompt the user to enter their name.
name = input("Please enter your name: ")

# This will print a greeting message with the user's name.
print("Hello, " + name + "!")
